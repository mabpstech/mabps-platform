"use client";

/**
 * Backward-compatible entry: Theme Studio replaces the legacy theme editor.
 * Existing imports of `@/components/website/theme-editor` continue to work.
 */
export { ThemeStudio as ThemeEditor, ThemeStudio } from "@/components/website/theme/theme-studio";
