/** URL-safe workspace slug from a display name or raw slug input. */
export function slugifyWorkspace(value: string): string {
  return value
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "")
    .slice(0, 48);
}
